<UserControl x:Class="BeatSaberBackUpTool.Views.DialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             Width="291"
             Height="167"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBlock HorizontalAlignment="Center" Text="{Binding Messege}" Margin="30"/>
            <Button Width="75" Command="{Binding OkCommand}" Content="OK" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
